name: Build and deploy

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: node-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: Build
      run: |
        npm install
        npm run deploy
